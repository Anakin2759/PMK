# API 实体句柄隔离改造规划

## 1. 目标

将 `src/api/` 作为稳定的公开 API 层，对外只暴露 `ui::entity`，不暴露 `entt::entity` 或 EnTT 相关依赖；将 `src/detail/` 作为内部实现层，继续使用 `entt::entity` 与 ECS 组件、Registry 交互。

改造完成后，外部用户只需要理解 `ui::entity`：

```cpp
using entity = std::uint32_t;
```

EnTT 类型只允许出现在内部实现、`detail` 层、系统层或必要的 `.cpp` 转换代码中。

## 2. 总体原则

### 2.1 公开 API 层

`src/api/` 下的公开接口应满足：

- 函数入参中的实体统一使用 `ui::entity`。
- 函数返回实体时统一返回 `ui::entity`。
- 公开头文件不得暴露 `entt::entity`。
- 公开头文件不得直接包含 EnTT 头文件。
- 公开头文件尽量不包含 `core/RuntimeFacade.hpp`、组件头、系统头等实现细节。
- 链式 DSL 入口继续基于 `ui::entity`。

### 2.2 内部 detail 层

`src/detail/` 下的内部接口应满足：

- 内部实体参数继续使用 `entt::entity`。
- 内部实体返回值继续使用 `entt::entity`。
- 访问 `Registry`、组件、Tag、系统、EnTT dispatcher 的实际逻辑放在 `detail` 层。
- `api/*.cpp` 只负责参数转换、返回值转换和转发调用。

### 2.3 转换边界

转换只发生在公开层和内部层的边界：

- API 入参：`ui::entity` 转为 `entt::entity`。
- Detail 返回：`entt::entity` 转为 `ui::entity`。
- `Result<entt::entity>` 转为 `Result<ui::entity>`。
- `ui::null_entity` 与 `entt::null` 必须保持一致。

## 3. 建议目录与职责

```text
src/api/       对外 API：ui::entity，不暴露 EnTT
src/detail/    内部 API：entt::entity，封装 ECS 具体实现
src/common/    公共类型、Result、错误码、策略、基础数据类型
src/systems/   系统实现，可使用 EnTT
```

建议新增内部转换头：

```text
src/detail/EntityCast.hpp
```

职责：

- `ToInternal(ui::entity) -> entt::entity`
- `ToPublic(entt::entity) -> ui::entity`
- `ToPublic(Result<entt::entity>) -> Result<ui::entity>`
- 对 `ui::null_entity` 与 `entt::null` 做一致性校验

该文件只允许被 `detail` 或 `api/*.cpp` 包含，不应被公开 API 头文件包含。

## 4. 命名空间规划

当前 `api` 与 `detail` 中存在同名命名空间，例如都使用 `ui::visibility`。为避免重载、链接和包含顺序问题，建议统一调整为：

| 层级 | 命名空间示例 | 实体类型 |
|------|--------------|----------|
| 公开 API | `ui::visibility` | `ui::entity` |
| 内部实现 | `ui::detail::visibility` | `entt::entity` |

所有 `src/detail/` 模块建议迁移到 `ui::detail::*` 命名空间，例如：

- `ui::detail::factory`
- `ui::detail::hierarchy`
- `ui::detail::visibility`
- `ui::detail::animation`
- `ui::detail::text`
- `ui::detail::table`
- `ui::detail::utils`

## 5. API 实现模式

公开层 `.cpp` 文件应采用薄封装模式。

示例：

```cpp
void Show(ui::entity entity)
{
	ui::detail::visibility::Show(ui::detail::ToInternal(entity));
}
```

返回实体示例：

```cpp
ui::entity CreateButton(const std::string& content, std::string_view alias)
{
	return ui::detail::ToPublic(ui::detail::factory::CreateButton(content, alias));
}
```

`Result<entity>` 示例：

```cpp
ui::Result<ui::entity> FindByAlias(std::string_view alias)
{
	return ui::detail::ToPublic(ui::detail::query::FindByAlias(alias));
}
```

## 6. 链式 DSL 要求

链式 DSL 是公开 API 的一部分，应继续面向 `ui::entity`。

要求：

- `ui::chains::Chain`、`AnyChain`、`operator|` 继续使用 `ui::entity`。
- `EntityAction` 在 `src/api/` 中绑定公开 API 函数。
- 不要在公开 DSL 中直接绑定 `ui::detail::*` 函数。
- `entity | Size(...) | BackgroundColor(...) | Show()` 语义保持不变。

如果 `detail` 层确实需要内部 DSL，可以保留内部版本，但不得通过公开头文件泄漏给外部用户。

## 7. 重点文件处理

### 7.1 `common/EntityTypes.hpp`

目标：成为 `ui::entity` 的单一公开定义。

建议：

- 保留 `#include <cstdint>`。
- 移除 EnTT 头文件包含。
- 保留 `using entity = std::uint32_t;`。
- 保留 `inline constexpr entity null_entity = 0xffffffffU;`。
- 与 `entt::null` 的一致性检查放入 `detail/EntityCast.hpp`。

### 7.2 `src/api/*.hpp`

逐个检查公开头文件：

- 将实体参数改为 `ui::entity` 或 `entity`。
- 将实体返回值改为 `ui::entity` 或 `entity`。
- 移除不必要的 EnTT、Registry、组件头依赖。
- 注释中的 `entt::entity` 改为 `ui::entity` 或“实体句柄”。

### 7.3 `src/api/*.cpp`

逐个改成转发层：

- 引入对应 `detail/*.hpp`。
- 引入 `detail/EntityCast.hpp`。
- 不直接访问 `Registry` 和组件。
- 不复制 `detail` 层实现逻辑。

### 7.4 `src/detail/*`

逐个确认：

- 使用 `entt::entity`。
- 命名空间迁移到 `ui::detail::*`。
- 保留 ECS 具体实现。
- 内部互调时优先调用 `ui::detail::*`。

## 8. 推荐实施顺序

按风险由低到高迁移：

1. 新增 `src/detail/EntityCast.hpp`。
2. 清理 `src/common/EntityTypes.hpp` 的 EnTT 依赖。
3. 迁移 `Visibility`。
4. 迁移 `Size`。
5. 迁移 `Layout`。
6. 迁移 `Animation`。
7. 迁移 `Text`。
8. 迁移 `Hierarchy`。
9. 迁移 `Factory`。
10. 迁移 `Table`。
11. 迁移 `Controls`。
12. 迁移 `Canvas`、`Icon`、`Image`。
13. 迁移 `Utils`。
14. 迁移 `Query`。
15. 迁移 `Shortcut`、`Timer`、`Theme`。

说明：

- `Visibility`、`Size`、`Layout` 多为 setter，适合先建立迁移模板。
- `Factory` 返回实体多，依赖广，应在基础模式稳定后处理。
- `Hierarchy`、`Table` 涉及多个实体参数和子实体关系，需要重点测试。
- `Utils` 被内部模块大量使用，建议后置迁移。

## 9. 验收标准

完成后应满足：

- `src/api/**/*.hpp` 不出现 `entt::entity`。
- `src/api/**/*.hpp` 不直接包含 `entt/`。
- 外部用户只需要使用 `ui::entity`。
- `src/api/*.cpp` 基本只做转换和转发。
- `src/detail/*` 承担实际 ECS 实现。
- 链式 DSL 正常工作。
- 示例程序可构建并运行。
- 单元测试可通过。

建议使用以下检查方向：

- 搜索 `src/api` 下是否仍有 `entt::entity`。
- 搜索 `src/api` 下是否仍有 `#include <entt` 或 `#include "entt`。
- 构建 Debug。
- 如启用测试，运行 `ui_tests`。

## 10. 风险点

### 10.1 命名空间冲突

如果 `api` 和 `detail` 都继续使用 `ui::xxx`，会导致函数重载、包含顺序和链接边界不清晰。建议优先迁移 `detail` 到 `ui::detail::xxx`。

### 10.2 公开头文件模板函数

如 `Hierarchy.hpp` 中的模板遍历函数，容易为了模板实现直接包含 Registry 和组件头。建议将取子节点逻辑下沉到 `detail`，公开模板只处理 `ui::entity` 列表或改为非模板回调接口。

### 10.3 `EntityAction` 绑定对象

公开链式 API 应绑定公开函数。如果绑定内部 `detail` 函数，会导致 DSL 再次依赖 `entt::entity`。

### 10.4 `Result<entity>` 转换

错误码必须原样保留，只转换成功值。

### 10.5 `null_entity` 一致性

必须确保 `ui::null_entity` 和 `entt::null` 的底层值一致，否则无效实体判断会出现隐蔽错误。

## 11. 单模块迁移模板

以 `Visibility` 为例：

1. 将 `src/detail/Visibility.hpp/.cpp` 命名空间改为 `ui::detail::visibility`。
2. 保持 `detail` 函数参数为 `entt::entity`。
3. 将 `src/api/Visibility.hpp` 保持为 `ui::entity`。
4. 将 `src/api/Visibility.cpp` 改为只调用 `ui::detail::visibility::*`。
5. 保持 `ui::chains::Show()` 等链式函数绑定 `ui::visibility::*`。
6. 构建验证。

