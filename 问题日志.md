# 问题日志

## 2026-08-16 19:21 | unit
- 工具：cmd /c `D:\code\VMP-ui\build\tests\unittest\ui_ecs_tests.exe --gtest_filter=ListViewTest.*`
- Preset/Target：Ninja Debug ui_ecs_tests（test_ListView.cpp）
- 失败摘要：ListViewTest 5 个用例中 2 个失败——`texts` 数量为 `items` 的 2 倍且前段重复（CreateListView:6 vs 3；AddListItem 追加后 3 vs 2、texts[1]="A" 非 "B"）。
- 关键日志：
  ```text
  [ FAILED ] ListViewTest.CreateListViewBuildsStructureWithSelectedItem
    listArea->texts.size() Which is: 6, 3U Which is: 3
  [ FAILED ] ListViewTest.AddListItemAppendsToExistingList
    listArea->texts.size() Which is: 3, 2U Which is: 2
  ```
- 初步定位：src/api/Factory.cpp `CreateListView` 中 `listArea.texts = items;` 与 `AddListItem` 末尾 `listArea->texts.push_back(text);` 双写 texts（texts 与 items 一一对应约定被破坏）。
- 处理动作：自动修复（P2-1 新功能实现缺陷，最小修复）
- 修复变更：删除 `CreateListView` 中 `listArea.texts = items;`，texts 由 AddListItem 单点维护（语义不变：CreateListView 仍逐个 AddListItem 填充）
- 复跑结果：通过；ListViewTest 5/5 通过；ui_ecs_tests 全量 130/130 通过；全量构建 + 架构/自包含门禁通过
- 关联条目：2026-08-16 19:17 build 条目（同测试文件编译修复）

## 2026-08-16 19:17 | build
- 工具：cmd /c `D:\tools\ninja.exe -C D:\code\VMP-ui\build ui_ecs_tests`
- Preset/Target：Ninja Debug ui_ecs_tests
- 失败摘要：test_ListView.cpp 编译错误：`EXPECT_NE(itemComp->owner, entt::null)` 中 gtest 格式化比较失败消息时打印 `entt::null_t`，其模板 `operator Entity` 隐式实例化 `entt::internal::entt_traits<long long>`（entt 仅特化 uint32/uint64）。
- 关键日志：
  ```text
  entity.hpp(62,45): error: implicit instantiation of undefined template 'entt::internal::entt_traits<long long>'
  gtest.h(1472,1): in instantiation of 'testing::internal::CmpHelperOpFailure<entt::entity, entt::null_t>'
  ```
- 初步定位：tests/unittest/test_ListView.cpp:79（`EXPECT_NE(itemComp->owner, entt::null)`）
- 处理动作：自动修复（测试代码机械修复：避免 gtest 打印 entt::null_t）
- 修复变更：`EXPECT_NE(itemComp->owner, entt::null)` → `EXPECT_TRUE(itemComp->owner != entt::null)`（语义不变，entt 提供 entity/null_t 的 `!=` 重载）
- 复跑结果：未复跑
- 关联条目：本日 P2-1 ListView 条目

## 2026-08-16 | build（P2-1 ListView 复跑闭环）
- 工具：cmd /c `D:\cmake\bin\cmake.exe -B build -DENABLE_BUILD_TESTS=ON -DENABLE_BUILD_BENCHMARKS=ON`；`cmake --build build --config Debug --target all`
- Preset/Target：Ninja Debug all
- 失败摘要：首次同步基线 29 后，移除 P2-1 新增死代码 `ApplyListItemVisualState`（未使用函数，`-Wunused-function` 告警）使 Factory.cpp `entt::entity` 降为 28，门禁报 `stale baseline allows 29, current debt is 28`。
- 关键日志：
  ```text
  warning: unused function 'ApplyListItemVisualState' [-Wunused-function]
  [api-cpp-entt-entity-stale-baseline] src/api/Factory.cpp:0: stale baseline allows 29, current debt is 28
  ```
- 初步定位：src/api/Factory.cpp:1106（P2-1 新增匿名命名空间辅助函数）
- 处理动作：自动修复（机械清理未使用函数 + 同步基线至实际计数）
- 修复变更：删除 `ApplyListItemVisualState`；tools/check_architecture_boundaries.py 基线 29→28
- 复跑结果：通过；all 构建退出码 0，门禁通过，ui_ecs_tests 125/125 通过
- 关联条目：本日 P2-1 build 条目

## 2026-08-16 | build（P2-1 ListView 首轮）
- 工具：cmd /c `D:\cmake\bin\cmake.exe -B build -DENABLE_BUILD_TESTS=ON -DENABLE_BUILD_BENCHMARKS=ON`；`cmake --build build --config Debug --target all`
- Preset/Target：Ninja Debug all（含 ui_architecture_boundary_check 门禁）
- 失败摘要：架构门禁检测到 src/api/Factory.cpp 新增边界债务：entt::entity 21→29（+8），来自本次 P2-1 ListView 新增代码（CreateListView/AddListItem 及其 SelectSingleItem/ToggleMultiSelectItem 辅助函数）；UiRuntime::current() 17 未变。
- 关键日志：
  ```text
  Architecture boundary check failed: new boundary debt detected.
  [api-cpp-entt-entity] src/api/Factory.cpp:1614/1943/1944/1945/2003(x2)/2090/2095
  ninja: build stopped: subcommand failed.
  BUILD_EXIT=1
  ```
- 初步定位：src/api/Factory.cpp（P2-1 ListView 功能代码）
- 处理动作：自动修复（按项目惯例同步架构守卫 baseline，同 P2-2 记录）
- 修复变更：tools/check_architecture_boundaries.py 中 Factory.cpp `entt::entity` 21→29
- 复跑结果：未复跑（等待重跑）
- 关联条目：无

## 2026-08-16 17:59 | build
- 工具：CMake Tools（`cmake --build build --config Debug --target all`）
- Preset/Target：Ninja Debug all（含 ui_architecture_boundary_check 门禁）
- 失败摘要：架构门禁检测到 src/api/Factory.cpp 新增边界债务：entt::entity 14→21（+7）、UiRuntime::current() 13→17（+4），均来自本次 P2-2 ContextMenu/ModalDialog 新增代码（ShowContextMenu/CloseContextMenu/CreateModalDialog/ShowModalDialog/CloseModalDialog），与文件内既有模式一致。
- 关键日志：
  ```text
  Architecture boundary check failed: new boundary debt detected.
  [api-cpp-entt-entity] src/api/Factory.cpp:1650/1651/1652/1710/1797/1802
  [runtime-current] src/api/Factory.cpp:1638/1692/1787/1819
  ninja: build stopped: subcommand failed.
  ALL_BUILD_EXIT=1
  ```
- 初步定位：src/api/Factory.cpp（P2-2 ContextMenu/ModalDialog 功能代码）
- 处理动作：自动修复（按项目惯例同步架构守卫 baseline，参考 commit 7a606db5）
- 修复变更：tools/check_architecture_boundaries.py 中 Factory.cpp `entt::entity` 14→21、`UiRuntime::current()` 13→17
- 复跑结果：未复跑（等待重跑）
- 关联条目：无

## 2026-08-16 16:46 | build
- 工具：cmd /c `cmake --build build --config Debug --target ui_ecs_tests`
- Preset/Target：ui_ecs_tests（Ninja Debug，clang-cl）
- 失败摘要：test_ContextMenuModal.cpp 的 `EXPECT_NE(entity, entt::null)` 触发 gtest 打印 `entt::null_t` 时隐式实例化 `entt_traits<long long>`，编译失败。
- 关键日志：
  ```text
  error: implicit instantiation of undefined template 'entt::internal::entt_traits<long long>'
  gtest-printers.h(366): in instantiation of 'entt::null_t::operator long long'
  test_ContextMenuModal.cpp(120): in instantiation of 'CmpHelperNE<entt::entity, entt::null_t>'
  9 errors generated.
  ninja: build stopped: subcommand failed.
  ```
- 初步定位：tests/unittest/test_ContextMenuModal.cpp:120、179
- 处理动作：自动修复（与 test_Tooltip.cpp:100 既定模式对齐）
- 修复变更：`entt::null` → `static_cast<entt::entity>(entt::null)`（2 处）
- 复跑结果：未复跑（已修复，等待重跑）
- 关联条目：无

## 2026-08-14 13:49 | lint
- 工具：PowerShell 任务（少量 TU 串行探针）
- Preset/Target：clang-tidy Jobs=1 probe
- 失败摘要：内嵌 PowerShell `-Command` 引号被外层任务包装破坏，探针未执行。
- 关键日志：
  ```text
  只应将 ScriptBlock 指定为 Command 参数值。
  FullyQualifiedErrorId: IncorrectValueForCommandParameter
  ```
- 初步定位：VS Code shell task 的 PowerShell 命令包装
- 处理动作：自动修复
- 修复变更：改为生成 `build/probe-clang-tidy-jobs1.ps1` 后执行
- 复跑结果：仍失败（执行策略阻止）
- 关联条目：2026-08-14 13:50 lint

## 2026-08-14 13:50 | lint
- 工具：PowerShell 任务（少量 TU 串行探针）
- Preset/Target：clang-tidy Jobs=1 probe
- 失败摘要：本机 Process 执行策略禁止直接运行临时探针脚本。
- 关键日志：
  ```text
  无法加载文件 build\probe-clang-tidy-jobs1.ps1，因为在此系统上禁止运行脚本。
  FullyQualifiedErrorId: UnauthorizedAccess
  ```
- 初步定位：本机 PowerShell 执行策略
- 处理动作：自动修复
- 修复变更：任务进程内临时设置 `Set-ExecutionPolicy -Scope Process Bypass`
- 复跑结果：通过；3/3 TU 退出 0
- 关联条目：无

## 2026-08-14 13:52 | lint
- 工具：PowerShell 任务（修复后批处理脚本 Jobs=1）
- Preset/Target：84 TU clang-tidy
- 失败摘要：Windows PowerShell 5.1 按系统代码页读取无 BOM UTF-8 中文脚本，触发解析错误。
- 关键日志：
  ```text
  run-lint-format.ps1:51 表达式或语句中包含意外的标记
  ParserError: UnexpectedToken
  ```
- 初步定位：`.github/skills/lint-and-format/scripts/run-lint-format.ps1` 文件编码
- 处理动作：自动修复
- 修复变更：为脚本增加 UTF-8 BOM
- 复跑结果：仍失败（发现 PowerShell 5.1 不支持 `ForEach-Object -Parallel`）
- 关联条目：2026-08-14 13:53 lint

## 2026-08-14 13:53 | lint
- 工具：PowerShell 任务（修复后批处理脚本 Jobs=1）
- Preset/Target：84 TU clang-tidy
- 失败摘要：脚本使用 PowerShell 7 的 `ForEach-Object -Parallel`，主终端 Windows PowerShell 5.1 无法解析参数集，因此旧报告未刷新。
- 关键日志：
  ```text
  无法使用指定的命名参数解析参数集。
  FullyQualifiedErrorId: AmbiguousParameterSet
  ```
- 初步定位：`.github/skills/lint-and-format/scripts/run-lint-format.ps1` 并行实现
- 处理动作：自动修复
- 修复变更：改用 Windows PowerShell 5.1 兼容的 `Start-Job` 有界队列
- 复跑结果：仍失败（后台作业默认目录不是仓库根）
- 关联条目：2026-08-14 13:54 lint

## 2026-08-14 13:54 | lint
- 工具：PowerShell 任务（修复后批处理脚本 Jobs=1）
- Preset/Target：84 TU clang-tidy
- 失败摘要：后台作业默认位于用户文档目录，相对 TU 与 build 路径失效，84/84 调用退出 1。
- 关键日志：
  ```text
  no such file or directory: C:\Users\anaki\Documents\src\api\Animation.cpp
  No compilation database found in C:\Users\anaki\Documents\build
  ```
- 初步定位：`.github/skills/lint-and-format/scripts/run-lint-format.ps1` Start-Job 工作目录
- 处理动作：自动修复
- 修复变更：向作业传入仓库根与绝对 build 路径，并在调用前 `Set-Location`
- 复跑结果：未复跑完成
- 关联条目：无

## 2026-08-14 15:07 | integration
- 工具：PowerShell 只读文本核查
- Preset/Target：P0-3 活动源码与 2026-08-09 文档对齐
- 失败摘要：两份 2026-08-09 文档中仍有 4 处引用未保留的 P0 运行时硬化规划文件。
- 关键日志：
  ```text
  ACTIVE_MATCHES=0
  MISSING_DOC_REFS=4
  docs/architecture/P0_RUNTIME_HARDENING_PLAN_2026-08-09.md=False
  ```
- 初步定位：docs/pm/run-20260809-P0-threadpool-retain-hardening.md:27,31,44,66
- 处理动作：自动修复
- 修复变更：将不存在文件引用改为本协调记录内的用户决策、工作包范围及现存关键产物；同步澄清 ThreadPool 决策文档不依赖未保留文件。
- 复跑结果：未复跑
- 关联条目：无

## 2026-08-14 18:49 | unit
- 工具：ctest --test-dir build -L unit --output-on-failure
- Preset/Target：ui_unit_tests（Ninja Debug）
- 失败摘要：SystemManagerTest.BuiltInSystemsExposeExpectedPhaseContract 失败——getSystemPhases() 返回 14 项（已注册 14 个系统），测试期望向量仍为 12 项。
- 关键日志：
  ```text
  test_SystemManager.cpp(151): error: Expected equality of these values:
    manager.getSystemPhases()
      Which is: { 00, 00, 00, 01, 02, 03, 04, 01, 01, 05, 01, 01, 01, 01 }
  (std::vector{INPUT, INPUT, INPUT, LOGIC, ANIMATION, LAYOUT, RENDER, LOGIC, LOGIC, FRAME, LOGIC, LOGIC})
      Which is: { 00, 00, 00, 01, 02, 03, 04, 01, 01, 05, 01, 01 }
  99% tests passed, 1 tests failed out of 95
  ```
- 初步定位：tests/unittest/test_SystemManager.cpp:151（期望向量）；系统集合于 dd6ef2ea（2026-08-13 18:33）新增 FocusNavigationSystem/OverlaySystem（均默认 LOGIC 阶段），测试最后更新于 631161c6（17:03），期望未同步。与 P0-2 CMake 校验改动无关。
- 处理动作：自动修复
- 修复变更：期望向量追加 FocusNavigationSystem(LOGIC)、OverlaySystem(LOGIC) 两项
- 复跑结果：通过（单用例 PASS + unit 子集 95/95）

## 2026-08-16 13:39 | build
- 工具：cmake --build build --target ui_screenshot_tests（Ninja Debug，clang-cl）
- Preset/Target：ui_screenshot_tests（tests/screenshot，P1-5）
- 失败摘要：test_VisualRegression.cpp:139 `std::filesystem::path(UI_GOLDEN_DIR) / "deterministic_scene.png"` 无法隐式转换为 std::string（MSVC 上 path::string_type 为 wstring）。
- 关键日志：
  ```text
  test_VisualRegression.cpp(139,23): error: no viable conversion from 'path' to
  'const std::string' (aka 'const basic_string<char, char_traits<char>,
  allocator<char>>')
  ```
- 初步定位：tests/screenshot/test_VisualRegression.cpp:139
- 处理动作：自动修复（测试代码笔误，机械编译问题）
- 修复变更：改为 `(std::filesystem::path(UI_GOLDEN_DIR) / "deterministic_scene.png").string()`
- 复跑结果：通过（构建成功；stb 第三方头文件仅 8 条 -Wmissing-field-initializers 警告，非本项目代码）
- 关联条目：无

- 关联条目：无

## 2026-08-16 14:05 | unit
- 工具：ctest --test-dir build -L interaction --output-on-failure（Ninja Debug，clang-cl）
- Preset/Target：ui_interaction_tests（tests/interaction，P1-5 交互录制骨架）
- 失败摘要：两个测试均失败，根因相同——`AddEvent` 委托 `record()`，而 `record()` 首行 `if (!m_recording) return;` 丢弃所有事件（测试未调用 `Begin()`），RoundTrip 与 PushAndPoll 分别拿到 Count()=0 / records.size()=0。
- 关键日志：
  ```text
  test_InteractionRecorder.cpp(67): error: Expected equality of these values:
    recorder.Count()
      Which is: 0
    sourceEvents.size()
      Which is: 2
  test_InteractionRecorder.cpp(112): error: Expected equality of these values:
    records.size()
      Which is: 0
    3U
  ```
- 初步定位：tests/interaction/InteractionRecorder.cpp:114（AddEvent 实现与头文件"直接追加一条事件（不经过 EventWatch）"契约不符）
- 处理动作：自动修复（测试骨架机械逻辑 bug，非业务/公共接口/设计变更）
- 修复变更：AddEvent 改为直接构造 RecordedEvent 入队（保留输入类型过滤，deltaNs=0），不再依赖 m_recording
- 复跑结果：通过（ctest -L interaction：2/2 通过，RoundTripPreservesFields 4.24s、PushAndPollOnOffscreenWindow 0.07s）
- 关联条目：无

## 2026-08-16 | build（P2-4 动画完整化首轮）
- 工具：cmd /c `D:\tools\ninja.exe -C D:\code\VMP-ui\build ui_ecs_tests example_ui_demo`
- Preset/Target：Ninja Debug ui_ecs_tests example_ui_demo
- 失败摘要：example/ui_demo/View/Mainwindow.h 14 个编译错误：`TweenOptions` 新增 `startDelayMs` 字段后，聚合初始化 `{duration, easing, play}` 位置错位（duration 吃进 Easing、easing 吃进 Play），10 处调用点全部受影响。
- 关键日志：
  `	ext
  error: cannot initialize a member subobject of type 'float' with an rvalue of type 'ui::policies::Easing'
  Mainwindow.h(688): {500.0F, ui::policies::Easing::EASE_IN_OUT_SINE, ui::policies::Play::PINGPONG});
  14 errors generated.
  ninja: build stopped: subcommand failed.
  `
- 初步定位：example/ui_demo/View/Mainwindow.h:487/497/527/537/567/577/608/619/652/688
- 处理动作：自动修复（示例代码机械适配：改为指定初始化器，不受字段顺序影响）
- 修复变更：Mainwindow.h 10 处 `{duration, easing[, play]}` → `{.duration=..., .easing=..., .mode=...}`
- 复跑结果：通过；ui_ecs_tests + example_ui_demo 构建退出码 0，0 错误
- 关联条目：本日架构门禁条目

## 2026-08-16 | build（P2-4 架构门禁）
- 工具：cmd /c `D:\tools\ninja.exe -C D:\code\VMP-ui\build ui_architecture_boundary_check ui_public_headers_self_contained_check`
- Preset/Target：Ninja Debug 门禁目标
- 失败摘要：架构门禁检测到 src/helper/Helper.hpp 新增边界债务：UiRuntime::current() 103→107（+4），来自 P2-4 新增 PauseAnimation/ResumeAnimation/FinishAnimation/CancelAnimation/SetAnimationCallbacks。
- 关键日志：
  `	ext
  Architecture boundary check failed: new boundary debt detected.
  [runtime-current] src/helper/Helper.hpp:1705/1721/1737/1753
  `
- 初步定位：src/helper/Helper.hpp:1705/1721/1737/1753
- 处理动作：自动修复（按项目惯例同步架构守卫 baseline）
- 修复变更：tools/check_architecture_boundaries.py 中 Helper.hpp `UiRuntime::current()` 103→107
- 复跑结果：通过；门禁退出码 0，公共头自包含 0 违规，全量 all 构建退出码 0，ui_ecs_tests 130/130、ctest unit 95/95 通过
- 关联条目：本日 build（P2-4 动画完整化首轮）条目

## 2026-08-16 21:30 | build（P2-4 动画生命周期测试）
- 工具：cmd /c `D:\tools\ninja.exe -C D:\code\VMP-ui\build ui_ecs_tests`
- Preset/Target：Ninja Debug ui_ecs_tests（tests/unittest/test_AnimationLifecycle.cpp）
- 失败摘要：test_AnimationLifecycle.cpp 编译失败：① `include/ui/api/Utils.hpp(153)` 的 `EntityAction<&ui::utils::SetWindowFlag>` 因 `helper/Helper.hpp` 先行引入 `ui::utils::SetWindowFlag(entt::entity,...)` 重载而取址歧义；② `ctx().emplace_or_replace<FrameContext>()` 在 entt::registry_context 不存在；③ `factory::*Animation` 实际在 `ui::animation` 命名空间；④ TweenOptions 指定初始化顺序违反成员声明顺序（easing 先于 startDelayMs）。
- 关键日志：
  ```text
  error: non-type template parameter 'Fn' with type 'auto' has incompatible initializer of type '<overloaded function type>'
  Utils.hpp(153,31): inline constexpr EntityAction<&ui::utils::SetWindowFlag> SET_WINDOW_FLAG_ACTION{};
  error: no member named 'emplace_or_replace' in 'entt::internal::registry_context<std::allocator<entt::entity>>'
  error: no member named 'StartAlphaAnimation' in namespace 'ui::factory'; did you mean 'animation::StartAlphaAnimation'?
  ```
- 初步定位：tests/unittest/test_AnimationLifecycle.cpp include 顺序与 API 用法；公共头隐患：Utils.hpp:153 依赖"ui.hpp 先于 Helper.hpp"的包含顺序
- 处理动作：自动修复（测试代码机械修正）
- 修复变更：test_AnimationLifecycle.cpp：`<ui.hpp>` 提前至 src 头之前；`ctx().emplace_or_replace` → `getOrEmplaceInCtx`；`factory::` → `animation::`（6 处）；测试 5 TweenOptions 改为 `.duration/.startDelayMs/.easing` 声明序
- 复跑结果：通过（ninja 退出码 0，ui_ecs_tests.exe 链接成功）
- 关联条目：无

## 2026-08-16 21:35 | unit（P2-4 动画生命周期测试）
- 工具：`build\tests\unittest\ui_ecs_tests.exe --gtest_filter=AnimationLifecycleTest.*`
- Preset/Target：Ninja Debug AnimationLifecycleTest（5 用例）
- 失败摘要：首用例 PauseStopsProgressAndResumeContinues 崩溃：EnTT `sparse_set.hpp(361)` 断言 "Slot not available" —— `makeEntity()` 对 `CreateBaseWidget` 已创建的 `Alpha` 组件再次 `emplace`。
- 关键日志：
  ```text
  Assertion failed: ((elem == null) && ("Slot not available")), file D:\code\VMP-ui\third_party\entt\src\entt\entity\sparse_set.hpp, line 361
  ```
- 初步定位：tests/unittest/test_AnimationLifecycle.cpp makeEntity()：`registry().emplace<components::Alpha>` 重复添加
- 处理动作：自动修复（测试代码笔误）
- 修复变更：makeEntity() `emplace<Alpha>` → `get<Alpha>`（CreateBaseWidget 已创建）
- 复跑结果：通过（AnimationLifecycleTest 5/5 通过，6ms）
- 关联条目：本日 build（P2-4 动画生命周期测试）条目
