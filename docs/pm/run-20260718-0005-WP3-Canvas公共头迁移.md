# WP3 Canvas 公共头迁移

- 日期：2026-07-18
- 状态：completed
- 前置：公共 `Color`、`Vec2`、Entity 与 Chains 已提供稳定路径

## 实施

- 新增稳定公共头 `include/ui/api/Canvas.hpp`。
- 删除旧权威路径 `src/api/Canvas.hpp`。
- `Canvas.cpp` 和 `include/ui.hpp` 切换到稳定路径。
- `src/CMakeLists.txt` 公共头清单切换到新路径。
- PublicLeafHeaders 增加 `<ui/api/Canvas.hpp>` 直接包含。
- 架构门禁将 `Canvas.hpp` 加入已迁移 API 头集合，禁止旧路径回归。

## 公共契约覆盖

新增 Runtime/GPU-free Painter 测试，固定 Painter 可由 `ui::entity` 构造、五个公开方法的函数类型及 fluent 返回自身语义。测试只提交单点路径，确保不会调用 `DrawPolyline()` 或访问 `UiRuntime`。

## 兼容性

命令式绘图函数、默认线宽、Painter 成员方法、EntityAction 和 Chain DSL 均保持不变。本批不修改 CanvasDrawList、贝塞尔细分算法、Renderer、Factory 或运行时命令记录行为。

## 验证

- Debug 全量构建：通过。
- `example_ui_demo` 编译及链接：通过。
- 架构门禁、umbrella header 和公开头检查：通过。
- 全量测试：174 passed / 0 failed。
- 指标：302 / 2 / 3 / 1。
- 用户修改的 `include/ui/api/Size.hpp` 未被本批改动。

## 残余风险

真实 Canvas 命令记录仍依赖 active `UiRuntime`，本批测试不覆盖内部 ECS 命令内容。`Entity.hpp` 仍转发到源码树，install/export consumer 和 CMake PUBLIC 边界仍未闭环。剩余 Controls、Factory、Utils 需单独处理 callback、Runtime 和 Application 类型边界。