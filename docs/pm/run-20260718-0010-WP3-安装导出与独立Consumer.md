# WP3 安装导出与独立 Consumer 闭环

- 日期：2026-07-18
- 状态：completed
- 工作包：WP3 构建与 SDK 边界

## 本批目标

为静态 UI SDK 建立可重定位的 CMake 安装包，并从隔离安装前缀验证真实静态链接闭包。

## 已完成

- 定义 `VMPUI` package，导出主目标 `VMPUI::ui`。
- 生成并安装 `VMPUIConfig.cmake`、`VMPUIConfigVersion.cmake`、`VMPUITargets.cmake`。
- 安装稳定 `include/`、`ui` 静态库、CMRC 字体资源归档和 `cmrc-base`。
- 安装静态库最终链接所需的 SDL3、Yoga、FreeType、HarfBuzz、EnTT、Eigen、spdlog package。
- 修正 Yoga 与 CMRC 安装接口的绝对路径，保持安装树可重定位。
- 新增 `tests/install_consumer`；仅通过 `find_package(VMPUI CONFIG REQUIRED)` 和 `VMPUI::ui` 消费 SDK。
- consumer 调用 out-of-line `CreateApplication()`，避免仅验证 header-only/inline 表面闭包。
- consumer 使用与 SDK 一致的 MSVC 静态运行库契约：Debug `/MTd`，其他配置 `/MT`。

## 验收结果

- Debug CMake Tools 构建：通过。
- 隔离安装：`cmake --install build --prefix build/install` 通过。
- 独立 consumer 配置：通过。
- 独立 consumer 编译与静态链接：通过。
- CTest：result code 0，176 项通过。
- 架构门禁：通过，指标保持 `302 / 0 / 0 / 1`。
- CMake 文件诊断：无错误。

## 结论

WP3 的公共头、PUBLIC 构建边界、安装导出和独立安装树 consumer 已全部闭环，完成度更新为 100%。WP9 的 WP3 前置阻塞解除；多平台、多编译器和多配置发布矩阵仍属于 WP9。
