# WP3 Table 公共头迁移

- 日期：2026-07-18
- 状态：completed
- 前置：公共 `Color`、`Policies`、`Entity` 与 Chains 已提供稳定依赖路径

## 实施

- 新增稳定公共头 `include/ui/api/Table.hpp`。
- 删除旧权威路径 `src/api/Table.hpp`。
- `Table.cpp` 和 `include/ui.hpp` 切换到稳定路径。
- `src/CMakeLists.txt` 公共头清单切换到新路径。
- PublicLeafHeaders 增加 `<ui/api/Table.hpp>` 直接包含。
- 架构门禁将 `Table.hpp` 加入已迁移 API 头集合，禁止旧路径回归。

## 兼容性

Table 函数、Action 和 Chain DSL 的签名及行为均未改变。本批保留 vector 参数的 move 捕获与单次消费语义，不修改 Table 组件、渲染逻辑、单元格控件 callback 或 Runtime 访问。

## 验证

- Debug 全量构建：通过。
- 架构门禁、umbrella header 和公开头检查：通过。
- 全量测试：173 passed / 0 failed。
- 指标：302 / 2 / 3 / 1。
- 构建末尾的 Windows introspection 提示不影响 result code 0；本批未新增 warning。

## 残余风险

`Entity.hpp` 仍转发到源码树 `common/EntityTypes.hpp`，项目也尚未提供 install/export consumer，因此本批不代表 SDK 边界闭环。剩余 Animation、Canvas、Controls、Factory、Utils 的依赖面更复杂，需逐项核验后迁移。