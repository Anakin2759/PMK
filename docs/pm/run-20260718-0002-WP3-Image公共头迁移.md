# WP3 Image 公共头迁移

- 日期：2026-07-18
- 状态：completed
- 前置：公共 `Color` 与 `Vec2` 已解除 Eigen 边界阻塞

## 实施

- 新增稳定公共头 `include/ui/api/Image.hpp`。
- 删除旧权威路径 `src/api/Image.hpp`。
- `Image.cpp` 和 `include/ui.hpp` 切换到稳定路径。
- `src/CMakeLists.txt` 公共头清单补入新路径。
- PublicLeafHeaders 增加 `<ui/api/Image.hpp>` 直接包含。
- 架构门禁将 `Image.hpp` 加入已迁移 API 头集合，禁止旧路径回归。

## 兼容性

`SetImagePath`、`SetImageTint`、`SetImageUV` 及 ImagePath/ImageTint/ImageUV DSL 的签名和行为均未改变。本批只改变物理头路径，不修改 Image 组件、资源加载或 Runtime 访问。

## 验证

- Debug 全量构建：通过。
- 架构门禁、umbrella header、MathTypes 独立头检查：通过。
- PublicLeafHeaders 定向测试：7 passed / 0 failed。
- 全量测试：173 passed / 0 failed。
- 指标：302 / 2 / 3 / 1。
- 既有 warning：`Timer.cpp` 忽略 nodiscard 返回值；本批未新增 warning。

## 残余风险

`Entity.hpp` 仍转发到源码树 `common/EntityTypes.hpp`，因此 Image 头迁移尚不等于 install-only consumer 验收。Table 是下一低风险物理头候选；Factory/Utils 仍涉及 Runtime/Application 等更复杂边界。
