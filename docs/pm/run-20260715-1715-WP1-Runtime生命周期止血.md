# 项目经理协调记录 - WP1 Runtime 生命周期止血

- 时间：2026-07-15 17:15
- 输入来源：用户明确要求按 `docs/architecture/ARCHITECTURE_REVIEW_AND_ROADMAP_2026-07-11.md` 的 Phase 1 / WP1 端到端实施
- 本轮范围：实现、测试、构建验证、路线图状态同步
- 验收标准：`tryCurrent()` 为 `noexcept`；Runtime 构造/析构不切换 current；Application 全生命周期持有 scope 且销毁顺序安全；Factory catch 不依赖 Runtime；生命周期测试覆盖指定场景；现有 CMake 测试与构建通过；不触达无关文件

## 已核对现状

- 2026-07-15 17:15 已重新读取近期被外部修改的架构路线文档。
- WP1 当前为 `active`，路线要求增加 `tryCurrent()`、修复 `CreateApplication()` catch、仅由 `UiRuntimeScope` 切换 current。
- `UiRuntime` 构造/析构当前直接写 `s_current`。
- `ApplicationImpl` 当前只有 `std::unique_ptr<UiRuntime>`，未持有长期 `UiRuntimeScope`。
- `CreateApplication()` 两个 catch 当前调用 `UiRuntime::current()`。

## 工作包

| # | 工作包 | Agent | 输入 | 产物 | 状态 |
|---|---|---|---|---|---|
| 1 | WP1 源码与单元测试落地 | 代码工厂 | 本文条目 #1；路线图 Phase 1 / WP1 | 源码变更、测试变更、交付报告 | 阻塞：当前会话未提供 agent 调度能力 |
| 2 | WP1 构建与测试闭环 | 测试构建闭环 | 条目 #1 交付报告；target=`ui_tests`；tests=`unit/api/ecs`，重点 Runtime 生命周期测试 | 测试报告、阶段结果、问题日志 | 待办（依赖 #1） |
| 3 | 路线图状态与证据同步 | 代码工厂 | 条目 #1/#2 产物；实施前已重读路线图 | 仅更新 WP1 状态、完成证据与残留项 | 待办（仅在验证完成后执行） |

## 工作包 #1 派发规格

- **目标**：完成用户列出的 1～5 项实现与测试，不改变 `current()` 的兼容调用形式。
- **依据**：`docs/architecture/ARCHITECTURE_REVIEW_AND_ROADMAP_2026-07-11.md` 的 3.1、3.2、Phase 1 / WP1；用户本轮确认的现状与目标。
- **允许文件**：
  - `src/core/UiRuntime.hpp`
  - `src/core/UiRuntimeScope.hpp`（仅在 noexcept/契约需要时）
  - `src/core/Application.cpp`
  - `src/api/Factory.cpp`
  - `tests/unittest/test_UiRuntime.cpp`（允许新增）
  - `tests/unittest/CMakeLists.txt`
- **禁止文件**：其他业务源码、第三方依赖、构建拓扑、公共协议、无关测试、无关文档；禁止未授权 Git 操作。
- **实现约束**：
  - `UiRuntime::tryCurrent()` 返回可空指针且 `noexcept`；`current()` 保持现有兼容接口。
  - `UiRuntime` 构造/析构不得修改 `s_current`。
  - `ApplicationImpl` 成员声明顺序必须保证 scope 在 runtime 之后构造、在 systems/事件清理期间有效，并在 runtime 之前销毁。
  - `CreateApplication()` catch 只能使用不依赖 Runtime 的 stderr 后备日志，且日志路径自身不得抛出。
  - 测试至少覆盖：无 current、构造不抢占、嵌套 scope 逐层恢复、scope/runtime 销毁后不留下悬挂 current。
- **验收标准**：静态自检无新增诊断；交付报告列出全部变更文件、关键生命周期顺序和未验证项。
- **失败升级条件**：发现必须触达允许范围外文件、`current()` 兼容语义需改变、或无法无 UB 地满足销毁顺序时，停止并返回待确认；空报告或越界修改视为失败，仅允许重派一次。

## 工作包 #2 派发规格

- **目标**：使用现有 CMake 构建目录完成 WP1 验证。
- **依据**：工作包 #1 交付报告。
- **目标配置**：现有 `build`；target=`ui_tests`（必要时先构建受影响的 `ui_unit_tests`）；tests=新增 Runtime 测试及现有 `unit/api/ecs` 测试集。
- **报告期望**：configure（若无需重配须明确）、build、定向测试、全量项目测试各阶段结果；报告路径；问题日志新增条数。
- **失败策略**：仅可自修测试登记/构建脚本笔误；业务或生命周期问题写入问题日志并升级，不扩大源码范围。
- **验收标准**：受影响 target 构建成功，新增测试全绿，现有测试无回归；或报告完整记录阻塞。
- **失败升级条件**：同阶段两次失败、环境/工具不可用、或失败指向范围外既有问题。

## 工作包 #3 派发规格

- **目标**：验证全绿后同步 WP1 状态与证据，避免路线图漂移。
- **允许文件**：`docs/architecture/ARCHITECTURE_REVIEW_AND_ROADMAP_2026-07-11.md`。
- **禁止文件**：其他文档和所有源码/测试。
- **前置条件**：编辑前再次重读该文档；仅依据 #1/#2 的实际产物更新，不宣称未验证事项完成。
- **验收标准**：WP1 状态、完成证据、剩余工作与测试结果一致。
- **失败升级条件**：文档再次发生外部修改或测试未闭环时停止更新。

## 调度时间线

- 17:15 重新读取架构路线图并核对 WP1 最新状态。
- 17:15 完成实现、验证和文档同步三个工作包的范围与验收拆分。
- 17:15 当前会话未暴露子 agent 调度能力，也未暴露任务/终端执行能力；按 PM 权限边界不能亲自改源码或运行构建，工作包 #1 暂停派发。

## 待用户决策

- [ ] 启用本会话的子 agent 调度能力（至少 `代码工厂`、`测试构建闭环`），以及 CMake 任务/终端执行能力；或切换到具备这些能力的主 agent 会话继续执行。

## 结论

- 状态：阻塞
- 关键产物：本协调记录及三个具备显式边界的工作包
- 下一工作包：#1 WP1 源码与单元测试落地
