# 项目经理协调记录 - P0 运行时契约

- 时间：2026-08-25（当前会话未提供精确时分，文件名以 0000 占位）
- 输入来源：`docs/ARCHITECTURE_REVIEW_PLAN_2026-08-25.md` 第 9 节及用户实施要求
- 本轮范围：现状核验、实现、最窄构建与测试闭环
- 验收标准：EventLoop 发布/排空协议、EventConnection Runtime 归属、回调快照/重入安全具备代码与测试证据；最窄相关构建和测试通过；不触达 RichText/SVG/Theme；无 Git 写操作

## 工作包

| # | 工作包 | Agent | 输入 | 产物 | 状态 |
|---|---|---|---|---|---|
| 1 | 只读核验 P0 三项当前实现状态 | Explore | 本文与架构评审第 3、9 节 | 代码证据摘要、建议的精确文件范围、证据不足项 | 阻塞：当前会话未提供子 Agent 调度能力 |
| 2 | 落地 P0 运行时契约 | 代码工厂 | 工作包 #1 证据；架构评审第 9 节 | 源码/测试变更与交付报告 | 待办 |
| 3 | 最窄相关构建与测试闭环 | 测试构建闭环 | 工作包 #2 交付报告 | 测试报告、阶段结果、问题日志新增条数 | 待办 |

## 工作包闸门

### #1 只读核验

- 目标：确认三项契约当前是否已部分或全部实现，不凭计划文档推断代码事实。
- 依据：`docs/ARCHITECTURE_REVIEW_PLAN_2026-08-25.md` 第 3、9 节。
- 允许文件：`src/utils/EventLoop.hpp`、`src/utils/MpscQueue.hpp`、`include/ui/api/Event.hpp`、`src/api/Event.cpp`、`src/helper/Helper.hpp`、`tests/unittest/`、`tests/support/`，以及仅为追踪符号所必需的 Runtime/Event bridge 定义文件。
- 禁止文件：RichText、SVG、Theme 及其他上层功能；禁止写文件、改代码、执行 Git。
- 验收标准：逐项列出现状、调用链、已有测试、缺口、证据路径；给出工作包 #2 的显式文件清单；证据不足项明确标记“保持不改”。
- 升级条件：Runtime 控制块或事件表实际归属跨出计划范围；公开 API 必须发生不兼容变更；无法从代码确认语义。

### #2 实现

- 目标：仅按 #1 已确认的证据修复 P0 三项契约并补回归测试。
- 依据：工作包 #1 证据摘要与架构评审第 9 节。
- 允许文件：由 #1 返回的精确清单限定，优先限制在计划列出的 7 个路径范围内。
- 禁止文件：范围外源码、RichText/SVG/Theme、无关重构、新依赖、CMake 拓扑改造、Git 写操作。
- 验收标准：EventLoop 无计数下溢且排空/丢弃语义可测；连接绑定稳定 Runtime 事件域且跨 Runtime/销毁后安全；派发采用书面快照语义并覆盖新增、断开、自断开、嵌套触发；输出包含变更文件列表的交付报告。
- 升级条件：需要更改公共控件 API；需要保存裸 `UiRuntime*`；无法在现有 C++23 结构中建立安全所有权；静态自检存在无法消除的相关错误。

### #3 验证

- 目标：执行与改动直接相关的最窄构建和 GoogleTest 测试，并稳定复跑相关并发/重入测试。
- 依据：工作包 #2 交付报告中的 target 与测试名称。
- 目标参数：preset/配置沿用现有 `build` Debug；target 以承载新增测试的最窄单元测试目标为准；tests 仅筛选 EventLoop、PublicEvent/EventConnection、callback snapshot/reentrancy 相关用例。
- 禁止事项：无关全仓格式化、无关测试修复、Git 写操作。
- 验收标准：configure（仅必要时）、最窄 target build、相关测试及稳定复跑全绿；否则报告路径、失败阶段、原始错误摘要与问题日志新增条数完整。
- 升级条件：业务/接口语义失败、竞态复现不稳定、需要扩大到全量构建才能定位、同阶段修复验证失败两次。

## 调度时间线

- 2026-08-25：确认请求涉及运行时生命周期、跨模块实现和测试闭环，进入 Full PM Path。
- 2026-08-25：读取架构评审第 3、9 节并建立三个聚焦工作包。
- 2026-08-25：因当前会话无子 Agent 调度工具，工作包 #1 未能派发，禁止由项目经理越权修改源码或运行构建。

## 待用户决策

- [ ] 启用包含 Explore、代码工厂、测试构建闭环的 Agent 调度能力后继续本协调记录。

## 结论

- 状态：阻塞
- 关键产物：本协调记录
- 下一工作包：#1 只读核验 P0 三项当前实现状态
