# 测试构建闭环报告 - 2026-08-16 15:41

## 概览
| 阶段 | 结果 | 备注 |
|---|---|---|
| Configure | 通过 | `cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=Debug -DENABLE_BUILD_TESTS=ON -DENABLE_BUILD_BENCHMARKS=ON`；纳入 tests/interaction 子目录 |
| Build | 通过 | target=ui_interaction_tests（修复后重新链接成功） |
| Test | 通过 | ctest -L interaction：2/2 通过 |
| Coverage | 跳过 | 未配置覆盖率 |
| Package | 跳过 | 未要求打包 |

## 失败与处理
| 阶段 | 名称 | 摘要 | 问题日志 |
|---|---|---|---|
| Test | InteractionRecorderTest.RoundTripPreservesFields | recorder.Count()=0，AddEvent 委托 record() 被 m_recording=false 短路 | 2026-08-16 14:05 unit |
| Test | InteractionRecorderTest.PushAndPollOnOffscreenWindow | 同根因，Load 后 records.size()=0 | 2026-08-16 14:05 unit |

## 自动修复
- `tests/interaction/InteractionRecorder.cpp`：`AddEvent` 不再委托 `record()`（其首行 `if (!m_recording) return;` 丢弃事件），改为直接构造 `RecordedEvent` 入队（保留输入类型过滤、deltaNs=0），与头文件“测试辅助：直接追加一条事件（不经过 EventWatch）”契约一致。属测试骨架机械逻辑修复，未触碰业务逻辑/公共接口/模块边界。

## 待人工处理
- [ ] 无
