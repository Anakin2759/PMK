# 测试构建闭环报告 - 2026-08-14 18:51

## 概览
| 阶段 | 结果 | 备注 |
|---|---|---|
| Configure | 通过 | Ninja Debug；`ENABLE_BUILD_TESTS=ON`、`ENABLE_BUILD_BENCHMARKS=ON`（CMakeCache 确认） |
| Build | 通过 | target=all（Ninja，27 步无错误，复跑确认 0 work + 门禁重跑） |
| Test | 通过 | ctest `-L unit`：95/95 通过（修复前 94/95，1 失败已修复） |
| Coverage | 跳过 | 本次未配置覆盖率目标 |
| Package | 跳过 | 未请求打包 |

## 验证范围（P0-2 剩余项）
- `tests/unittest/CMakeLists.txt` 与 `tests/benchmark/CMakeLists.txt` 已确认包含
  `ui_validate_sources_exist` / `add_ui_test_executable` 前置校验函数，且全部
  `add_executable` 已替换为 `add_ui_test_executable`。
- 构建目录已用 `-DENABLE_BUILD_TESTS=ON -DENABLE_BUILD_BENCHMARKS=ON` 重新配置。

## 门禁结果
| 门禁 | 结果 |
|---|---|
| 公共头自包含门禁（`[UI Distribution] Public header self-containment check`） | 通过：0 violations（include/ui/** + include/ui.hpp） |
| 架构门禁（`[UI Architecture] Architecture boundary check`） | 通过：UiRuntime::current() 341 调用、PUBLIC internal include 0、无新增 P0 边界债务 |
| 编译错误 | 无（构建退出码 0） |

## 测试结果
- ctest 可发现 231 个测试；标签：api / ecs / integration / stress / unit。
- `ctest --test-dir build -L unit --output-on-failure`：95/95 通过，8.70s。
- 修复前：94/95，失败用例 `SystemManagerTest.BuiltInSystemsExposeExpectedPhaseContract`。

## 失败与处理
| 阶段 | 名称 | 摘要 | 问题日志 |
|---|---|---|---|
| unit | SystemManagerTest.BuiltInSystemsExposeExpectedPhaseContract | 测试期望向量 12 项 vs 实际注册 14 系统（getSystemPhases 返回 14 项）；FocusNavigationSystem/OverlaySystem 于 dd6ef2ea 加入后未同步期望 | 2026-08-14 18:49 |

## 自动修复
- `tests/unittest/test_SystemManager.cpp`：期望向量追加
  `SystemPhase::LOGIC // FocusNavigationSystem`、`SystemPhase::LOGIC // OverlaySystem`
  （两个系统均使用 `ISystem::getPhase()` 默认 LOGIC，注册顺序与实际一致）。
- 与 P0-2 CMake 校验改动无关（预存过期期望，非本次引入）。

## FATAL_ERROR 探针（临时目录，已清理）
- 在 `%TEMP%\ui_p02_validate_probe` 复刻校验函数并引用 `does_not_exist.cpp`：
  configure 失败，`CONFIGURE_EXIT=1`，报错
  `[P0-2] 目标 probe_missing 引用的源文件不存在: .../does_not_exist.cpp`，调用栈
  指向 `ui_validate_sources_exist` / `add_ui_test_executable`。
- 正向对照：补回源文件后 configure 成功（`CONFIGURE_EXIT=0`），无误报。
- 临时目录已删除，工作区零污染。

## 待人工处理
- 无
