# 测试构建闭环报告 - 2026-08-16 21:35

## 概览
| 阶段 | 结果 | 备注 |
|---|---|---|
| Configure | 通过 | cmake.exe -B build -DENABLE_BUILD_TESTS=ON -DENABLE_BUILD_BENCHMARKS=ON，4.8s |
| Build | 通过 | ninja ui_ecs_tests，首次失败（见下），修复后退出码 0 |
| Test | 通过 | ui_ecs_tests.exe --gtest_filter=AnimationLifecycleTest.*，5/5 通过（6ms） |
| Coverage | 跳过 | 项目无覆盖率配置 |
| Package | 跳过 | 用户未要求打包 |

## 失败与处理
| 阶段 | 名称 | 摘要 | 问题日志 |
|---|---|---|---|
| Build | test_AnimationLifecycle.cpp 编译 | Utils.hpp SetWindowFlag 取址歧义 / ctx().emplace_or_replace 不存在 / factory:: 命名空间错误 / TweenOptions 指定初始化顺序 | 2026-08-16 21:30 build 条目 |
| Test | PauseStopsProgressAndResumeContinues | EnTT "Slot not available"：makeEntity 重复 emplace Alpha | 2026-08-16 21:35 unit 条目 |

## 自动修复
- `tests/unittest/test_AnimationLifecycle.cpp`
  - `<ui.hpp>` 提前至 `src/core/UiRuntime.hpp` / `src/systems/TweenSystem.hpp` 之前（规避 Helper.hpp 对 `ui::utils::SetWindowFlag` 的重载污染，与 test_TweenSystem.cpp 惯例一致）
  - `registry().ctx().emplace_or_replace<FrameContext>()` → `registry().getOrEmplaceInCtx<FrameContext>()`
  - `factory::StartAlphaAnimation/PauseAnimation/ResumeAnimation/StopAnimation/FinishAnimation/SetAnimationCallbacks` → `animation::`（`ui::animation`）共 6 处
  - 测试 5 `TweenOptions` 指定初始化改为成员声明序 `.duration → .startDelayMs → .easing`
  - `makeEntity()`：`emplace<Alpha>` → `get<Alpha>`（CreateBaseWidget 已创建 Alpha）

## 待人工处理
- [ ] 无

## 备注
- 测试用例列表：PauseStopsProgressAndResumeContinues / OnCompleteFiresWhenOnceFinishes / StopAnimationFiresOnCancel / FinishWithSettleWritesEndValueAndFiresComplete / StartDelayHoldsProgressUntilElapsed，5/5 通过
- 公共头隐患（已记录）：`include/ui/api/Utils.hpp:153` 的 `SET_WINDOW_FLAG_ACTION` 依赖包含顺序，任何先包含 `helper/Helper.hpp` 的 TU 会触发取址歧义，建议后续用 `static_cast` 显式定签名（本次未改公共接口）
