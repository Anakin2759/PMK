# 测试构建闭环报告 - 2026-08-16 16:28

## 概览
| 阶段 | 结果 | 备注 |
|---|---|---|
| Configure | 通过 | build 目录已配置（Ninja + Debug + ENABLE_BUILD_TESTS=ON）；全量构建时自动 Re-running CMake 成功 |
| Build | 通过 | target=all，退出码 0；公共头自包含门禁 0 violations；架构门禁无新增 P0 债务 |
| Test | 通过 | screenshot 3/3、interaction 2/2、unit 95/95 |
| Coverage | 跳过 | 未配置覆盖率 |
| Package | 跳过 | 未要求打包 |

## 失败与处理
| 阶段 | 名称 | 摘要 | 问题日志 |
|---|---|---|---|
| - | - | 无失败 | 无新增条目 |

## 自动修复
- 无（本次新增文件无编译/链接/测试错误，无需最小修复）

## 待人工处理
- [ ] 无

---

## 回归明细

### 1. 全量构建（Ninja all）
- 命令：`cmake --build build --config Debug --target all`
- 退出码：**0**
- 公共头门禁：
  ```text
  [UI Distribution] Checking public headers have zero third-party dependencies
  Public headers checked: include/ui/** + include/ui.hpp (0 violations)
  Public header self-containment check passed: zero third-party dependencies.
  ```
- 架构门禁：
  ```text
  [UI Architecture] Checking P0 boundary debt does not grow
  UiRuntime::current() calls: 341
  PUBLIC internal include paths: 0
  PUBLIC internal dependency links: 0
  queued event frame dispatch sites: 1
  Architecture boundary check passed: no new P0 boundary debt.
  ```
- 备注：本次构建重新编译了 `tests/interaction/InteractionRecorder.cpp` + `test_InteractionRecorder.cpp` 并重链接 `ui_interaction_tests.exe`（P1-5 新增/修改文件），无错误。CMake 重新配置时出现 Google Benchmark 的
  `Building introspection files on Windows requires BUILD_SHARED_LIBS` 提示，为已知良性信息，不影响构建（退出码 0）。

### 2. 截图回归（-L screenshot）
- 命令：`ctest --test-dir build -C Debug -L screenshot --output-on-failure`
- 结果：**3/3 通过**
  - ScreenshotCaptureTest.WritesPngAndReloadsWithExpectedPixels
  - ScreenshotCaptureTest.CompareSurfacesDetectsDifference
  - VisualRegressionTest.DeterministicSceneMatchesGoldenWithinTolerance
- 与预期 3 项一致。

### 3. 交互回归（-L interaction）
- 命令：`ctest --test-dir build -C Debug -L interaction --output-on-failure`
- 结果：**2/2 通过**
  - InteractionRecorderTest.RoundTripPreservesFields
  - InteractionRecorderTest.PushAndPollOnOffscreenWindow
- 与预期 2 项一致。

### 4. 单元测试（-L unit）
- 命令：`ctest --test-dir build -C Debug -L unit --output-on-failure`
- 结果：**95/95 通过**（0 失败），与预期 95 项一致。

### 5. CI workflow 检查（.github/workflows/cmake-multi-platform.yml · build-linux job）
| 检查项 | 结论 |
|---|---|
| Configure 含 `-DENABLE_BUILD_TESTS=ON` | ✅ 有 |
| Screenshot 回归步骤 `ctest --test-dir build -L screenshot --output-on-failure` | ✅ 有 |
| Interaction 回归步骤 `ctest --test-dir build -L interaction --output-on-failure` | ✅ 有 |
| Build 步骤 `cmake --build build --config Release --parallel`（含测试目标） | ✅ 有 |
| `-L unit` 步骤 | ➖ 无（不在 P1-5 要求范围，本地已单独跑 95/95） |

- CI 结论：**build-linux job 已完整纳入 P1-5 截图/交互回归**；本地 Debug + clang-cl 的验证结果与 CI 的 Release + GCC 15 环境存在配置差异，最终以 CI 实际运行为准（本机不做 Release 全量重配）。
