# 测试构建闭环报告 - 2026-08-14 12:29

## 概览
| 阶段 | 结果 | 备注 |
|---|---|---|
| Configure | 跳过 | 使用现有 `build`；Ninja、Debug、clang-cl |
| Build | 通过 | `cmake --build build --config Debug --target all`；退出码 0 |
| Test | 跳过 | 本轮要求为全量 all 构建与构建期门禁验证 |
| Coverage | 跳过 | 用户未要求，未重配覆盖率 |
| Package | 跳过 | 用户未要求；未提交/推送 |

## 关键结果
- `ui_public_headers_self_contained_check`：通过；检查 `include/ui/**` 与 `include/ui.hpp`，第三方依赖违规 0。
- `ui_architecture_boundary_check`：通过；未新增 P0 架构边界债务。
- 架构指标：`UiRuntime::current()` 调用 341；PUBLIC 内部 include 0；PUBLIC 内部依赖链接 0；排队事件帧派发点 1。
- 全量 `all` 目标完成；最终链接目标包括 `tests/unittest/ui_api_tests.exe`。
- 第一次运行已成功完成主要构建输出；随后再次运行任务确认增量收敛并获得退出码 0。

## 失败与处理
| 阶段 | 名称 | 摘要 | 问题日志 |
|---|---|---|---|
| - | - | 无失败 | 未新增 |

## 自动修复
- 无。

## 待人工处理
- [ ] 无

## 本轮新增修改文件
- `docs/test-reports/run-20260814-1229.md`（本报告）

构建前后业务源码状态一致；未因本轮验证修改源码、测试或构建脚本。未提交或推送。
