# 测试构建闭环报告 - 2026-08-16 09:49

## 概览
| 阶段 | 结果 | 备注 |
|---|---|---|
| Configure | 通过 | 沿用现有 build 目录（Ninja Debug + clang-cl，ENABLE_BUILD_TESTS=ON，ENABLE_CLANG_TIDY=OFF），未重新配置 |
| Build | 通过 | `cmake --build build`（Ninja all），退出码 0；源码早于 build.ninja，全图 up-to-date |
| Test | 通过 | stress 5/5；unit 95/95，均 100% |
| Coverage | 跳过 | 无覆盖率配置变更 |
| Package | 跳过 | 用户未要求打包 |

## 验证内容

P1-4（core::EventLoop 双模式帧调度：FIXED_RATE 默认 + EVENT_DRIVEN + 锁帧接口）全量验证。涉及文件：
- `src/core/EventLoop.hpp` / `src/core/EventLoop.cpp`：`FrameScheduleMode`、`setTargetFrameRate`/`targetFrameRate`、`setFrameScheduleMode`/`frameScheduleMode`、`WakeSchedulerIfEventDriven`、双模式 `frameSchedulerLoop`
- `tests/unittest/test_CoreEventLoopScheduling.cpp`：新增 3 项 `CoreEventLoopSchedulingTest`
- `tests/unittest/CMakeLists.txt`：`ui_eventloop_stress_tests` 纳入 `test_CoreEventLoopScheduling.cpp`

### 1. 全量构建（Ninja all）
| 检查项 | 结果 |
|---|---|
| Ninja all 退出码 | 0 |
| 公共头自包含门禁（`ui_public_headers_self_contained_check`） | 通过：include/ui/** + ui.hpp 0 违规 |
| 架构边界门禁（`ui_architecture_boundary_check`） | 通过：无新增 P0 边界债务（UiRuntime::current() 341 处，无新增） |

### 2. stress 测试（ctest -L stress）5/5 通过，CTEST_EXIT=0
| 测试 | 结果 | 耗时 |
|---|---|---|
| EventLoopStressTest.SingleTaskPingDoesNotLoseWakeup | Passed | 0.03s |
| EventLoopStressTest.MultiProducerBurstAllDrain | Passed | 0.03s |
| CoreEventLoopSchedulingTest.FixedRateDeliversFramesOnInterval | Passed | 0.08s |
| CoreEventLoopSchedulingTest.EventDrivenWakesOnInvoke | Passed | 0.12s |
| CoreEventLoopSchedulingTest.TargetFrameRateCanBeChanged | Passed | 0.05s |

### 3. unit 回归（ctest -L unit）95/95 通过，CTEST_EXIT=0
100% tests passed, 0 tests failed out of 95（总耗时 10.44s）。

## 失败与处理
| 阶段 | 名称 | 摘要 | 问题日志 |
|---|---|---|---|
| - | - | 无失败 | 无新增条目 |

## 自动修复
- 无

## 待人工处理
- [ ] 无（未提交/推送，按用户要求；本报告生成于构建验证之后）
